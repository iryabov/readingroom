<?xml version='1.0' encoding='UTF-8'?>
<query xmlns="http://n2oapp.net/framework/config/schema/query-4.0">
    <list>
        <sql>
            select :select
            from book b
            where :filters
            order by b.created_at desc
        </sql>
    </list>
    <count count-mapping="[0][cnt]">
        <sql>
            select count(*) as cnt
            from book b
            where :filters
        </sql>
    </count>

    <fields>
        <field id="id">
            <select mapping="[id]">b.id as id</select>
            <filters>
                <eq filter-id="id">b.id = :id</eq>
            </filters>
        </field>
        <field id="title">
            <select mapping="[title]"
                    normalize="T(com.github.iryabov.readingroom.service.UiUtilsKt).shorten(#this, 50)">b.title as title</select>
        </field>
        <field id="authors" domain="string">
            <select mapping="[authors]"
                    normalize="T(com.github.iryabov.readingroom.service.UiUtilsKt).shorten(#this, 50)">array_to_string(b.authors, ', ', '*') as authors</select>
        </field>
        <field id="cover">
            <select mapping="[cover]">b.cover as cover</select>
        </field>
        <field id="rating">
            <select mapping="[rating]">(select avg(r.rate) from rating r where r.book_id = b.id) as rating</select>
        </field>
    </fields>
</query>
